# Node.js dependencies
node_modules/

# npm lockfiles (we don't commit them in skill repos)
package-lock.json
.npmrc

# Python cache / build artifacts
__pycache__/
*.py[cod]
*$py.class
*.so
.Python/
.venv/
venv/
.pytest_cache/
.mypy_cache/

# Editor / OS
.DS_Store
.idea/
.vscode/
*.swp
*.swo
*.bak
*~

# Local secrets / env
.env
.env.local
*.local

# Build / cache artifacts
dist/
build/
.cache/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Coverage
coverage/
.nyc_output/

# Hermes curator / hub state (do NOT commit; local skill-management only)
skills/.bundled_manifest
skills/.bundled_manifest.*
skills/.curator_backups/
skills/.curator_ledger.jsonl
skills/.curator_state
skills/.usage.json
skills/.hub/
