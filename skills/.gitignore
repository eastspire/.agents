# Hermes-curator sidecar files -- never commit
.usage.json
.usage.json.lock
.curator_state
.curator_ledger.jsonl

# Throwaway PR-body drafts (prevent accidental commit; never landed in a PR)
.tmp-pr-body-*.md
